{
  "name": "cypress-automation-program",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api_tests": "npx cypress run --spec cypress/e2e/1-getting-started/api.cy.js",
    "ui_tests": "npx cypress run --spec cypress/e2e/1-getting-started/cypressProgram.cy.js --reporter cypress-junit-reporter"
  },
  "author": "Anton_Rak",
  "license": "ISC",
  "dependencies": {
    "cypress": "^11.0.1",
    "mocha": "^10.1.0"
  },
  "devDependencies": {
    "cypress-junit-reporter": "^1.3.1",
    "cypress-mochawesome-reporter": "^3.2.3"
  }
}
